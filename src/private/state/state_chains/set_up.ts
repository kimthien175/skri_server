import { SocketPackage } from "../../../types/socket_package.js";

class Setup{
    static async join(socketPkg: SocketPackage){
        // get room data first

    }

    static async disconnect(socketPkg: SocketPackage){

    }

    static async reconnect(socketPkg: SocketPackage){
        
    }
}